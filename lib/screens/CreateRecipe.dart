import 'package:flutter/material.dart';

class CreateRecipe extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create Recipe')),
      body: Center(child: Text('Create Recipe Page')),
    );
  }
}
